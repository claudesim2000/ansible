- hosts: serveur01
  tasks:
    - name: module de chk
    - package_facts:
      manager: yum

# Install packages
- name: Installation test
  debug:
    yum:
      pkg: "{{ item }}"
      state: latest
    become: true
  when: "{{ item }} not in ansible_facts.packages"
  with_items:
      - telnet
      - mailx
      - postfixadmin
